import { action } from '@storybook/addon-actions';
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import "./style-overrides.css";

import { FlowModeler } from "../src/index";

<Meta title='FlowModeler (options)' component={FlowModeler} />

# FlowModeler
## Focus: Display options

<Props of={FlowModeler} exclude={Object.keys(FlowModeler.propTypes).filter(propName => propName !== "options")} />

----

### Vertical Alignment: "top"

<Preview>
    <Story name="verticalAlign: top">
        <FlowModeler
            flow={{
                firstElementId: "1",
                elements: {
                    "1": { data: { label: "Step A" }, nextElementId: "2" },
                    "2": { data: { label: "Q1" }, nextElements: [
                        { id: "3.1", conditionData: { label: "Yes" } },
                        { id: "3.2", conditionData: { label: "No" } }] },
                    "3.1": { data: { label: "Opt. B" }, nextElementId: "4" },
                    "3.2": { data: { label: "Opt. C" }, nextElementId: "4" },
                    "4": { data: { label: "Q2" }, nextElements: [
                        { id: "5.1", conditionData: { label: "Yes" } },
                        { id: "5.2", conditionData: { label: "No" } },
                        { id: "5.3", conditionData: { label: "N/A" } }] },
                    "5.1": { data: { label: "Opt. D" } },
                    "5.2": { data: { label: "Opt. E" } },
                    "5.3": { data: { label: "Opt. F" } }
                }
            }}
            options={{ verticalAlign: "top" }}
            renderStep={({ data }) => <span>{data.label}</span>}
            renderGatewayConditionType={({ data }) => <label>{data.label}</label>}
            renderGatewayConditionValue={({ data }) => <label>{data.label}</label>}
        />
    </Story>
</Preview>

### Vertical Alignment: "middle" (default)
If no `options`/`options.verticalAlign` prop is defined, the paths will be vertically centered.

<Preview>
    <Story name="verticalAlign: middle">
        <FlowModeler
            flow={{
                firstElementId: "1",
                elements: {
                    "1": { data: { label: "Step A" }, nextElementId: "2" },
                    "2": { data: { label: "Q1" }, nextElements: [
                        { id: "3.1", conditionData: { label: "Yes" } },
                        { id: "3.2", conditionData: { label: "No" } }] },
                    "3.1": { data: { label: "Opt. B" }, nextElementId: "4" },
                    "3.2": { data: { label: "Opt. C" }, nextElementId: "4" },
                    "4": { data: { label: "Q2" }, nextElements: [
                        { id: "5.1", conditionData: { label: "Yes" } },
                        { id: "5.2", conditionData: { label: "No" } },
                        { id: "5.3", conditionData: { label: "N/A" } }] },
                    "5.1": { data: { label: "Opt. D" } },
                    "5.2": { data: { label: "Opt. E" } },
                    "5.3": { data: { label: "Opt. F" } }
                }
            }}
            renderStep={({ data }) => <span>{data.label}</span>}
            renderGatewayConditionType={({ data }) => <label>{data.label}</label>}
            renderGatewayConditionValue={({ data }) => <label>{data.label}</label>}
        />
    </Story>
</Preview>

----

### Vertical Alignment: bottom

<Preview>
    <Story name="verticalAlign: bottom">
        <FlowModeler
            flow={{
                firstElementId: "1",
                elements: {
                    "1": { data: { label: "Step A" }, nextElementId: "2" },
                    "2": { data: { label: "Q1" }, nextElements: [
                        { id: "3.1", conditionData: { label: "Yes" } },
                        { id: "3.2", conditionData: { label: "No" } }] },
                    "3.1": { data: { label: "Opt. B" }, nextElementId: "4" },
                    "3.2": { data: { label: "Opt. C" }, nextElementId: "4" },
                    "4": { data: { label: "Q2" }, nextElements: [
                        { id: "5.1", conditionData: { label: "Yes" } },
                        { id: "5.2", conditionData: { label: "No" } },
                        { id: "5.3", conditionData: { label: "N/A" } }] },
                    "5.1": { data: { label: "Opt. D" } },
                    "5.2": { data: { label: "Opt. E" } },
                    "5.3": { data: { label: "Opt. F" } }
                }
            }}
            options={{ verticalAlign: "bottom" }}
            renderStep={({ data }) => <span>{data.label}</span>}
            renderGatewayConditionType={({ data }) => <label>{data.label}</label>}
            renderGatewayConditionValue={({ data }) => <label>{data.label}</label>}
        />
    </Story>
</Preview>
